# =============================================================================
# RU Liquidity Sentinel — полные зависимости
# =============================================================================
# Установка всего (рекомендуется):
#   pip install -r requirements.txt
#
# Установка через pyproject.toml (все экстры):
#   pip install -e ".[models,dashboard,dev]"
#
# Python >= 3.10 обязателен.
# =============================================================================

# -----------------------------------------------------------------------------
# [A] Ядро пайплайна (ru-liquidity-sentinel)
# -----------------------------------------------------------------------------
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
pyarrow>=14.0           # Parquet I/O (артефакты)

# -----------------------------------------------------------------------------
# [B] Модели (опциональная экстра: models / dev)
# -----------------------------------------------------------------------------
shap>=0.44              # GradientExplainer: SHAP-атрибуция по модулям

# -----------------------------------------------------------------------------
# [C] NSVM (нейросетевая сигма-вариационная модель, pipeline.py / nsvm_model.py)
# -----------------------------------------------------------------------------
torch>=2.0              # LSTM-двухголовочная архитектура (mu + log_var)

# -----------------------------------------------------------------------------
# [D] Дашборд Streamlit (опциональная экстра: dashboard)
# -----------------------------------------------------------------------------
streamlit>=1.30
altair>=5.0
seaborn>=0.13

# -----------------------------------------------------------------------------
# [E] Слой парсеров (parsers/requirements.txt)
# -----------------------------------------------------------------------------
requests>=2.31          # HTTP-запросы к CBR / Minfin / Roskazna
openpyxl>=3.1           # Чтение Excel-файлов (m3 OFZ, m5 Roskazna)
xlrd>=2.0               # Устаревшие .xls (CBR)
beautifulsoup4>=4.12    # HTML-парсинг (налоговый календарь m4)
lxml>=4.9               # Бэкенд для bs4 + xpath
tenacity>=8.2           # Retry-логика HTTP-запросов
python-dateutil>=2.9    # Разбор дат из публикаций регуляторов
click>=8.1              # CLI для скриптов fetch_m*.py
tqdm>=4.66              # Прогресс-бары при загрузке

# -----------------------------------------------------------------------------
# [F] Сборка пакета
# -----------------------------------------------------------------------------
setuptools>=68
wheel

# -----------------------------------------------------------------------------
# [G] Dev / тестирование (опциональная экстра: dev)
# -----------------------------------------------------------------------------
pytest>=7.4
pytest-cov>=4.1
ruff>=0.4               # Линтер / форматтер
